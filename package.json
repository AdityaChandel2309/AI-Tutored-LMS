{
  "name": "lms",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Workforce Intelligence & Knowledge Platform",
  "workspaces": ["api", "web"],
  "scripts": {
    "build": "turbo run build",
    "build:dev": "turbo run build && node scripts/assemble-dist.js",
    "dev": "turbo run dev --filter=web --",
    "bootstrap": "bash scripts/bootstrap.sh",
    "bootstrap:reset": "bash scripts/bootstrap.sh --reset",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.16.0",
    "fast-check": "^3.22.0"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@10.0.0"
}
